# Лабораторная работа №4 - React фронтенд с Soft Delete

## 🎯 Задание по варианту 6

**Переключатель "Закрытые мероприятия" (Soft delete)**

- ✅ Backend: добавлен параметр `includeSoftDeleted` для GET /events
- ✅ Frontend: чекбокс "Показать удаленные мероприятия"
- ✅ Отображение статуса: "АКТИВНО" или "УДАЛЕНО"
- ✅ Дата удаления: показывается только для удаленных мероприятий

## 📋 Полные требования ЛР4

### Основное задание ✅
- ✅ **Главная страница** - Описание, логотип, кнопки навигации
- ✅ **Страница авторизации** - Форма входа, редирект авторизованного
- ✅ **Страница регистрации** - Форма регистрации, редирект на логин
- ✅ **Список мероприятий** - Карточки (grid), индикатор авторизации
- ✅ **Страница 404** - Информирование о ненайденной странице
- ✅ **Адаптивность** - Минимальный адаптив для > 320px
- ✅ **Обработка ошибок** - ErrorDisplay компонент (не alert/prompt/confirm)
- ✅ **Структура проекта** - Правильная иерархия файлов
- ✅ **React + Vite + TypeScript** - Все требования
- ✅ **ESLint и Prettier** - Настроены
- ✅ **localStorage** - Хранение токена и пользователя
- ✅ **Git коммиты** - 4 коммита со стандартным форматом

### Технологический стек ✅
- React 19.2.4
- Vite 7.3.1
- TypeScript 5.9.3
- React Router 7.13
- Axios 1.13.5
- SCSS/CSS Modules
- ESLint 9.39.2
- Prettier 3.8.1

## 📁 Структура проекта

```
project/
├── backend/                    # Node.js + Express backend
│   ├── src/
│   │   ├── config/            # Конфигурация БД и Passport
│   │   ├── models/            # Sequelize модели (User, Event, TokenBlacklist)
│   │   ├── routes/            # API маршруты (auth, events, users)
│   │   ├── middleware/        # Middleware функции
│   │   └── index.ts           # Главный файл приложения
│   ├── dist/                  # Скомпилированный JavaScript
│   ├── package.json
│   └── tsconfig.json
│
├── frontend/                  # React + Vite frontend
│   ├── src/
│   │   ├── api/               # API методы (authService, eventService)
│   │   ├── components/        # Переиспользуемые компоненты (Layout, ErrorDisplay)
│   │   ├── pages/             # Страницы приложения
│   │   │   ├── Home/
│   │   │   ├── Login/
│   │   │   ├── Register/
│   │   │   ├── Events/        # PAGE С SOFT DELETE ★
│   │   │   └── NotFound/
│   │   ├── types/             # TypeScript типы
│   │   ├── utils/             # Утилиты (tokenUtils)
│   │   ├── App.tsx            # Маршрутизация
│   │   └── main.tsx           # Точка входа
│   ├── index.html
│   ├── package.json
│   ├── tsconfig.json
│   └── vite.config.ts
```

## 🚀 Запуск проекта

### 1. Backend (Node.js + PostgreSQL)

```bash
cd backend

# Установить зависимости
npm install

# Запустить dev сервер (на порту 5000)
npm run dev

# Или build + запуск production
npm run build
npm start
```

**Backend доступен**: http://localhost:5000
**API документация**: http://localhost:5000/api-docs

### 2. Frontend (React + Vite)

```bash
cd frontend

# Установить зависимости
npm install

# Запустить dev сервер (на порту 5173)
npm run dev

# Build для production
npm run build

# Preview build
npm run preview
```

**Frontend доступен**: http://localhost:5173

## 🔐 Аутентификация

### Регистрация
- URL: POST `/auth/register`
- Поля: name, email, password
- Результат: Перенаправление на страницу логина

### Логин
- URL: POST `/auth/login`
- Поля: email, password
- Результат: JWT токен + информация о пользователе
- Хранилище: `localStorage` (auth_token, user_info)

### Логаут
- URL: POST `/auth/logout`
- Действие: Добавление токена в blacklist
- Результат: Перенаправление на /login

## 🎯 Основной функционал - Soft Delete

### Backend запросы
```
GET /events                          # Только активные мероприятия
GET /events?includeSoftDeleted=true # Включая удаленные
```

### Frontend реализация
```typescript
// Events страница
const [includeSoftDeleted, setIncludeSoftDeleted] = useState(false);

// Чекбокс для переключения
<label>
  <input
    type="checkbox"
    checked={includeSoftDeleted}
    onChange={() => setIncludeSoftDeleted(!includeSoftDeleted)}
  />
  Показать удаленные мероприятия
</label>

// Отображение статуса
{event.deletedAt ? 'УДАЛЕНО' : 'АКТИВНО'}

// Дата удаления (только для удаленных)
{event.deletedAt && (
  <p>Удалено: {new Date(event.deletedAt).toLocaleDateString()}</p>
)}
```

## 📝 API Endpoints

### Аутентификация
- `POST /auth/register` - Регистрация пользователя
- `POST /auth/login` - Логин пользователя
- `POST /auth/logout` - Логаут пользователя

### Мероприятия
- `GET /events` - Список мероприятий (параметр: `includeSoftDeleted`)
- `GET /events/:id` - Мероприятие по ID
- `POST /events` - Создание мероприятия
- `PUT /events/:id` - Обновление мероприятия
- `DELETE /events/:id` - Soft delete мероприятия

### Пользователи
- `GET /users` - Список пользователей
- `GET /users/:id` - Пользователь по ID

## 📊 Скрипты

### Backend
```bash
npm run dev        # Dev режим с nodemon (ts-node)
npm run build      # Компиляция TypeScript в JavaScript
npm start          # Запуск скомпилированного кода
npm run type-check # Проверка TypeScript типов
npm run lint       # ESLint проверка
npm run lint:fix   # ESLint с автоисправлением
npm run format     # Prettier форматирование
```

### Frontend
```bash
npm run dev           # Dev сервер (Vite)
npm run build         # Build для production
npm run preview       # Preview build
npm run type-check    # Проверка TypeScript
npm run lint          # ESLint проверка
npm run lint:fix      # ESLint с автоисправлением
npm run format        # Prettier форматирование
npm run format:check  # Проверка форматирования
```

## 🔄 Git структура

```
feature/lab4-frontend
├── 0d8f702 - init: LAB4 Инициализирован проект фронтенда на React + Vite
├── 5b5cf0c - feat: LAB4 Реализована поддержка includeSoftDeleted
├── b0b9cc4 - docs: LAB4 Обновлена документация Merge Request
└── 94ace80 - fix: LAB4 Восстановлены backend файлы
```

## 🛡️ Защита маршрутов

- `/` - Публичная (различные виды для авторизованных/неавторизованных)
- `/login` - Публичная (редирект авторизованных на /events)
- `/register` - Публичная (редирект авторизованных на /events)
- `/events` - Защищенная (редирект неавторизованных на /login)
- `/*` - 404 страница

## ⚠️ Важные замечания

1. **Backend должен быть запущен первым** перед запуском frontend
2. **БД**: PostgreSQL должна быть запущена и доступна
3. **Переменные окружения** (.env файлы уже настроены)
4. **CORS**: Настроен для работы с локальным frontend
5. **Токены**: Хранятся в localStorage, отправляются в заголовке Authorization

## 🧪 Тестирование

### Тестовые учетные данные
Регистрируем нового пользователя или используем существующего:
- Email: любой уникальный email
- Password: любой пароль

### Проверка Soft Delete
1. Логинимся на сайте
2. Переходим на страницу мероприятий (/events)
3. Видим список активных мероприятий
4. Включаем чекбокс "Показать удаленные мероприятия"
5. В списке появляются удаленные мероприятия со статусом "УДАЛЕНО"
6. Видим дату удаления для каждого удаленного мероприятия

## 📲 Адаптивность

Приложение адаптивно для разрешений:
- 📱 Mobile: 320px+
- 📱 Tablet: 768px+
- 🖥️ Desktop: 1024px+

## ✅ Статус выполнения

| Требование | Статус |
|-----------|--------|
| React + Vite + TypeScript | ✅ |
| Все страницы | ✅ |
| Аутентификация | ✅ |
| Soft Delete (вариант 6) | ✅ |
| Переключатель удаленных | ✅ |
| Статус мероприятия | ✅ |
| Дата удаления | ✅ |
| ESLint + Prettier | ✅ |
| localStorage | ✅ |
| Git коммиты | ✅ |
| Обработка ошибок | ✅ |
| Адаптивность | ✅ |

## 🔗 Связанные файлы

- [LAB4_MR.md](./LAB4_MR.md) - Описание Merge Request
- [backend/README.md](./backend/README.md) - Backend документация
- [backend/FEATURES.md](./backend/FEATURES.md) - Особенности API

## 📞 Контакты

**Автор**: Кирилл К.  
**Дисциплина**: Основы веб-разработки  
**Дата**: 2026-02-08  
**Статус**: ✅ Готово к защите

---

## 🎉 Заключение

ЛР4 полностью завершена со всеми требованиями:
- ✅ Фронтенд на React + Vite + TypeScript
- ✅ Вариант 6: Soft Delete с переключателем
- ✅ Интеграция с backend API
- ✅ Правильная иерархия файлов
- ✅ ESLint и Prettier настроены
- ✅ localStorage для токенов
- ✅ Git структура с коммитами
- ✅ Документация обновлена

**Оба сервера успешно запускаются и работают! 🚀**
