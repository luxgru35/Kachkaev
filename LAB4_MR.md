# ЛР4: Фронтенд на React - Merge Request

## 📝 Описание работы

Создан полнофункциональный фронтенд приложения для управления мероприятиями на основе React + Vite + TypeScript. Реализованы все требуемые страницы, интеграция с API, авторизация с использованием JWT токенов, а также функциональный вариант 6 - переключатель для отображения удаленных мероприятий (soft delete).

## ✅ Выполненные требования

### Основное задание (100%)

✓ **Структура проекта:**
- React + Vite + TypeScript
- ESLint и Prettier настроены
- Правильная иерархия папок: pages, components, api, utils, types

✓ **Страницы приложения:**
1. **Главная (Home)** - описание приложения, кнопки авторизации/регистрации, индикатор авторизации
2. **Авторизация (Login)** - форма входа, редирект на список мероприятий при успехе
3. **Регистрация (Register)** - форма регистрации, редирект на авторизацию после успеха
4. **Список мероприятий (Events)** - отображение карточек, авторизация обязательна
5. **404** - страница для неправильных маршрутов

✓ **Функциональность:**
- React Router для маршрутизации
- Axios для работы с API
- localStorage для хранения JWT токена
- Перехватчики на 401 ошибки с редиректом
- Обработка ошибок на странице (ErrorDisplay компонент)
- Адаптивный дизайн (работает на экранах от 320px)

✓ **Стилизация:**
- CSS Modules с SCSS
- Flex и Grid для верстки
- Отзывчивый дизайн
- Красивое оформление карточек и форм

### Функциональный вариант 6: Soft Delete ✓

**Требования:**
- ✓ Переключатель для отображения удаленных мероприятий
- ✓ Поддержка параметра `includeSoftDeleted` в запросе к API
- ✓ Отображение дополнительного поля "Дата удаления" для удаленных мероприятий
- ✓ Визуальное отличие удаленных мероприятий (специальный статус и стиль)

**Реализация:**
1. На странице Events добавлен чекбокс "Показать удаленные мероприятия"
2. При изменении состояния чекбокса отправляется новый запрос с параметром `includeSoftDeleted=true`
3. Backend обновлен для обработки этого параметра - если transmit, то включает мероприятия где `deletedAt` не NULL
4. На карточке удаленного мероприятия:
   - Отображается статус "УДАЛЕНО" вместо "АКТИВНО"
   - Добавляется граница красного цвета
   - Показываются с пониженной прозрачностью
   - Отображается дата и время удаления

## 🏗️ Структура проекта

```
frontend/
├── src/
│   ├── api/
│   │   ├── axios.ts          # Инстанс Axios с перехватчиками
│   │   ├── authService.ts    # Авторизация и регистрация
│   │   └── eventService.ts   # Работа с мероприятиями (с поддержкой soft delete!)
│   ├── components/
│   │   ├── ErrorDisplay.tsx  # Компонент для отображения ошибок
│   │   └── Layout.tsx        # Основной Layout с навигацией
│   ├── pages/
│   │   ├── Home/
│   │   ├── Login/
│   │   ├── Register/
│   │   ├── Events/           # Реализация варианта 6 с чекбоксом soft delete
│   │   └── NotFound/
│   ├── utils/
│   │   └── tokenUtils.ts     # Работа с localStorage и токенами
│   ├── types/
│   │   └── index.ts          # TypeScript типы и интерфейсы
│   ├── App.tsx
│   ├── main.tsx
│   └── vite-env.d.ts         # Типы для Vite переменных
├── tsconfig.json             # Конфиг TypeScript с path aliases
├── vite.config.ts
├── eslint.config.js
├── .prettierrc.json
└── package.json
```

## 🔑 Ключевые компоненты

### Events компонент (Вариант 6)
```typescript
// Поддержка параметра для soft delete
const [includeSoftDeleted, setIncludeSoftDeleted] = useState(false);
const data = await eventService.getAllEvents({ includeSoftDeleted });

// Визуализация статус удаленных мероприятий
const eventStatus = event.deletedAt ? 'УДАЛЕНО' : 'АКТИВНО'

// Отображение дату удаления
{event.deletedAt && (
  <div className={styles.detail}>
    <span className={styles.label}>🗑️ Удалено:</span>
    <span>{formatDate(event.deletedAt)}</span>
  </div>
)}
```

### API интеграция
```typescript
// eventService с поддержкой soft delete
getAllEvents(params?: EventListParams): Promise<Event[]> {
  return axiosInstance.get('/events', {
    params: { 
      ...(params?.includeSoftDeleted && { includeSoftDeleted: true })
    }
  })
}
```

## 🔄 Backend обновления

**Файл:** `backend/routes/events.ts`

Добавлена поддержка параметра `includeSoftDeleted`:
- Добавлен параметр в интерфейс `QueryParams`
- Обновлена логика фильтрации в GET /events:
  - Если `includeSoftDeleted=true`, включает удаленные мероприятия
  - По умолчанию показывает только активные (где `deletedAt = null`)

## 📋 Git структура

```
init: LAB4 Инициализирован проект фронтенда на React + Vite + TypeScript
feat: LAB4 Реализована поддержка параметра includeSoftDeleted
docs: LAB4 Обновлено описание Merge Request
```

## 🛠️ Технологический стек

**Frontend:**
- React 19.2.4
- TypeScript 5.9.3
- Vite 7.3.1
- React Router DOM 7.13.0
- Axios 1.13.5
- SASS 1.72.0
- ESLint 9.39.2
- Prettier 3.8.1

**Backend обновления:**
- Express TypeScript поддержка
- Sequelize ORM c soft delete (deletedAt)

## 📦 Установка и запуск

### Frontend:
```bash
cd frontend
npm install
npm run dev
# Приложение откроется на http://localhost:5173
```

### Backend:
```bash
cd backend
npm install
npm run dev
# Сервер запустится на http://localhost:3000
```

## 🎯 Примеры использования

### Регистрация и авторизация:
1. Перейти на страницу "/" (главная)
2. Нажать "Регистрация"
3. Заполнить форму (имя, email, пароль)
4. Нажать "Регистрация"
5. Перенаправится на "/login"
6. Ввести email и пароль
7. Попадете на "/events"

### Просмотр soft delete (Вариант 6):
1. На странице Events есть чекбокс "Показать удаленные мероприятия"
2. По умолчанию показывает только активные
3. При включении чекбокса также показываются удаленные
4. Удаленные имеют визуальные отличия:
   - Красная граница
   - Статус "УДАЛЕНО"
   - Отображается дата/время удаления

## 🔐 Безопасность

- Токен хранится в localStorage
- Автоматический редирект при 401 ошибке
- Авторизация обязательна для доступа к мероприятиям
- CSRF защита через проверку токена в заголовками

## 📱 Адаптивность

- Минимальная ширина: 320px
- Flex верстка для мобильных
- Media queries для планшетов и десктопов
- Тестировано на разных разрешениях

## ✨ Дополнительные улучшения

- Красивая обработка ошибок с autoclose
- Индикаторы загрузки
- Плавные переходы и анимации
- Единообразный дизайн
- Подробные комментарии в коде

## 🎓 Выводы

ЛР4 успешно реализована со всеми требованиями. Вариант 6 (soft delete) полностью функционален - пользователь может переключаться между просмотром только активных или всех мероприятий включая удаленные. Фронтенд полностью типизирован на TypeScript, имеет правильную структуру проекта, красивый пользовательский интерфейс и работает корректно с backend API.

---

**Статус**: ✅ Готово к мержу

**Ветка**: feature/lab4-frontend

**Коммиты**: 3 (иник + фит + документация)
